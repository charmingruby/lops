apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lops.fullname" . }}-test-connection"
  labels:
    {{- include "lops.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lops.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
