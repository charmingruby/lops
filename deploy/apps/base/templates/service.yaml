apiVersion: v1
kind: Service
metadata:
  name: {{ include "lops.fullname" . }}
  labels:
    {{- include "lops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lops.selectorLabels" . | nindent 4 }}
